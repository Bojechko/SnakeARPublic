using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// по какому принципу выбрано имя? обычно так называют интерфейсы
public class iBone : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // пустые event functions тоже будут вызываться в том числе каждый кадр
    // Update is called once per frame
    void Update()
    {
        
    }
}
